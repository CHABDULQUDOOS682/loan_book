<nav class="sticky top-0 z-50 backdrop-blur-md bg-gray-900/80 border-b border-blue-400/30 shadow-lg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <!-- Logo/Title -->
      <div class="flex items-center space-x-2">
        <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M4 4a2 2 0 00-2 2v4a2 2 0 002 2V6h10a2 2 0 00-2-2H4zm2 6a2 2 0 012-2h8a2 2 0 012 2v4a2 2 0 01-2 2H8a2 2 0 01-2-2v-4zm6 4a2 2 0 100-4 2 2 0 000 4z" clip-rule="evenodd" />
        </svg>
        <%= link_to root_path, class: "text-2xl font-bold bg-gradient-to-r from-violet-600 to-blue-500 bg-clip-text text-transparent" do %>
          <h1>QuantumLend</h1>
        <% end %>
      </div>

      <!-- User Controls -->
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <%= button_to "Go To Dashboard", book_loans_path, method: :get,
                        class: "px-4 py-2 rounded-full bg-gradient-to-r from-violet-600 to-blue-500 text-white hover:shadow-lg hover:shadow-violet-500/30 transition-all duration-300" %>

          <%= button_to "Sign out", destroy_user_session_path, method: :delete,
                        class: "px-4 py-2 rounded-full bg-gradient-to-r from-violet-600 to-blue-500 text-white hover:shadow-lg hover:shadow-violet-500/30 transition-all duration-300" %>
          <div class="w-8 h-8 rounded-full bg-gradient-to-r from-violet-600 to-blue-500 flex items-center justify-center text-white">
            <%= current_user.name[0].upcase %>
          </div>
        <% else %>
          <%= link_to 'Sign in', new_user_session_path, class: "px-4 py-2 rounded-full bg-gradient-to-r from-violet-600 to-blue-500 text-white hover:shadow-lg hover:shadow-violet-500/30 transition-all duration-300" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>